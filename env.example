# Local development environment variables
# For Docker deployment, see env.docker.example

GRPC_HOST=0.0.0.0
GRPC_PORT=50051

# IDP Configuration (LDAP)
IDP_TYPE=ldap
IDP_HOST=localhost
IDP_PORT=389
IDP_BASE_DN=dc=desaidentity,dc=com
IDP_USERS_DN=ou=People,dc=desaidentity,dc=com
IDP_BIND_DN=cn=admin,dc=desaidentity,dc=com
IDP_BIND_PASS=password
IDP_USE_TLS=false

# Storage Configuration
STORAGE_PATH=./data
STORAGE_IN_MEMORY=false

